# React TanStack Example

Minimal React example using TanStack Router and TanStack Query.

This example uses `ReactDIContext` to inject awilixify providers and sibling components through a system-owned `deps` prop.

```bash
npm install
npm run dev
```
