export function AboutPage() {
	return (
		<section className="panel compact">
			<h2>About</h2>
			<p className="muted">
				This example uses ReactDIContext to inject services and sibling
				components through the system-owned deps prop.
			</p>
		</section>
	);
}
